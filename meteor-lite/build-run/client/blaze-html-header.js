// the contents of this file is injected into the top of all converted HTML files (with this line removed)
import { HTML } from "@meteor/htmljs";
import { Blaze } from "@meteor/blaze";
import { Spacebars } from "@meteor/spacebars";
// needed in case the HTML has <body> tags
import { Meteor } from "@meteor/meteor";
